import React from 'react';
import {
  SafeAreaView,
  StyleSheet,
  ScrollView,
  View,
  Text,
} from 'react-native';
import Navigation from "./src/config/Navigation";

const App = () => {
  return (
      <Navigation />
  );
};

const styles = StyleSheet.create({
});

export default App;
